% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClassTopics2.R
\name{ClassTopics}
\alias{ClassTopics}
\title{Fits a supervised topic model using integrated LDA and NMF to data with
binary or categorical response variable}
\usage{
ClassTopics(
  counts,
  response,
  K = 6,
  shape = 4,
  sigma_eta = 0.5,
  betadir = TRUE,
  alpha_beta = 0.05,
  lambda_ridge = 0,
  n_iter_EM = 10,
  verbose_EM = TRUE,
  iter_warmup = 1000,
  iter_sampling = 1000,
  chains = 3,
  cores = 3,
  seed = 123,
  control = list(adapt_delta = 0.8, max_treedepth = 10),
  verbose = TRUE
)
}
\arguments{
\item{counts}{count matrix}

\item{response}{character or factor vector on the class labels}

\item{K}{number of topics to be modeled (default = \code{6})}

\item{shape}{scalar prior shape of the NMF coefficients (default = 4)}

\item{sigma_eta}{scalar prior sd of the coefficients of the eta matrix
(default = 0.5)}

\item{betadir}{logical, controls whether beta is estimated with Dirichlet
prior (default = \code{TRUE})}

\item{alpha_beta}{numeric hyperparameter of the symmetric Dirichlet
distribution if \code{betadir = TRUE} (default = 0.05)}

\item{lambda_ridge}{numeric ridge-penalty weight applied to the
regression coefficients \code{eta} (default = \code{0},
i.e. no ridge penalty)}

\item{n_iter_EM}{number of iterations of the EM pre-processing (default = \code{10})}

\item{verbose_EM}{logical that controls whether EM likelihood is printed
(default = \code{TRUE})}

\item{iter_warmup}{number of warm-up iterations for each chain
(default = \code{1000})}

\item{iter_sampling}{number of sampling iterations for each chain
(default = \code{1000})}

\item{chains}{number of chains to run (default = \code{3})}

\item{cores}{number of CPU cores to use (default = \code{3})}

\item{seed}{numeric seed for reproducibility (default = \code{123})}

\item{control}{control parameters for the Stan algorithm}

\item{verbose}{logical that controls whether the function prints information
regarding the algorithm steps (default = \code{TRUE})}
}
\value{
cmdstan model to interpret with the function ClassTopics_results
}
\description{
Fits a supervised topic model using integrated LDA and NMF to data with
binary or categorical response variable
}
\details{
\code{counts} ~ Poisson(\code{lambda}), \code{lambda = H \%*\% W}    (NMF component)
\code{response} ~ Categorical(softmax(\code{theta[i,] \%*\% eta}))  (supervised component)

Parameters (matching Stan file names exactly):
\code{H} : D x K  -- observation topic loadings
\code{W} : K x V  -- topic-variable profiles

Derived quantities (computed as transformed parameters in Stan,
NOT passed as initial values):
\code{theta} : D x K  -- topic proportions per observation
\code{beta}  : K x V  -- variable distributions per topic
}
\examples{

## Generate synthetic dataset

df <- generate_CTdata()

# Remove zero-count columns
zero_cols <- colSums(df@X) == 0
X <- df@X[, !zero_cols, drop = FALSE]

# Model fitting
\donttest{
fit <- ClassTopics(counts = X, response = df@Y)

# Posterior results extraction
results <- ClassTopics_results(fit = fit, true_response = df@Y)
}

}
