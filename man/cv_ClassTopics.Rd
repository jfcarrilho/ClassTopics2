% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClassTopics2.R
\name{cv_ClassTopics}
\alias{cv_ClassTopics}
\title{K-Fold Cross-Validation for ClassTopics Models (Parallel)}
\usage{
cv_ClassTopics(
  counts,
  response,
  k_folds = 5,
  K_topics = 3,
  shape = 4,
  shape_test = 1,
  sigma_eta = 0.5,
  betadir = TRUE,
  alpha_beta = 0.05,
  seed = 123,
  verbose_per_fold = TRUE,
  iter_warmup = 1000,
  iter_sampling = 1000,
  n_iter_EM = 4,
  n_iter_EM_test = 4,
  test_stan = TRUE,
  lambda_ridge = 0,
  chains = 3,
  cores = 3,
  control = list(adapt_delta = 0.8, max_treedepth = 10),
  ...
)
}
\arguments{
\item{counts}{count matrix}

\item{response}{character or factor vector on the class labels}

\item{k_folds}{number of folds to create for cross-validation (default = 5)}

\item{K_topics}{number of topics to be modeled (default = 3)}

\item{shape}{scalar prior shape of the NMF coefficients (default = 4)}

\item{shape_test}{scalar prior shape of the H matrix on test sets (default = 4)}

\item{sigma_eta}{scalar prior sd of the coefficients of the eta matrix}

\item{betadir}{logical, controls whether beta is estimated with Dirichlet
prior (default = \code{TRUE}) or, if \code{FALSE}, with Gamma prior}

\item{alpha_beta}{numeric hyperparameter of the symmetric Dirichlet
distribution if \code{betadir = TRUE} (default = 0.05)}

\item{seed}{numeric seed for reproducibility (default = 123)}

\item{verbose_per_fold}{Logical, print details for each fold (default: TRUE)}

\item{iter_warmup}{number of warm-up iterations for each chain
(default = 1000)}

\item{iter_sampling}{number of sampling iterations for each chain
(default = 1000)}

\item{n_iter_EM}{number of iterations of the EM pre-processing (default = 4)}

\item{n_iter_EM_test}{number of EM iterations used when fitting theta on
each fold's held-out (test) data (default = 4)}

\item{test_stan}{logical, if \code{TRUE} (default) test-fold topic proportions
are fit via Stan (see \code{\link[=predict_ClassTopics_stan]{predict_ClassTopics_stan()}});
if \code{FALSE}, via EM only (see \code{\link[=predict_ClassTopics_EM]{predict_ClassTopics_EM()}})}

\item{lambda_ridge}{numeric ridge-penalty weight applied to the
regression coefficients \code{eta} (default = 0, i.e. no
ridge penalty)}

\item{chains}{number of chains to run per fold and within the final model (default = \code{3})}

\item{cores}{number of CPU cores to use per fold and within the final model (default = \code{3})}

\item{control}{control parameters for the Stan algorithm}

\item{...}{Additional arguments passed to \code{\link[=ClassTopics]{ClassTopics()}} and
\code{\link[=predict_ClassTopics_stan]{predict_ClassTopics_stan()}} / \code{\link[=predict_ClassTopics_EM]{predict_ClassTopics_EM()}}}
}
\value{
A \link[=cvCTprediction_plusfm-class]{cvCTprediction_plusfm} object with per-fold and pooled
train/test accuracy, per-category accuracy, the pooled test confusion
matrix, an overfitting diagnostic, pooled out-of-fold predictions, and
the final model fit on the full dataset.
}
\description{
Runs stratified k-fold cross-validation for the supervised topic model
fit by \code{\link[=ClassTopics]{ClassTopics()}}, dispatching each fold's fit plus one final
fit on the full dataset as concurrent background jobs via the
\code{future} package.
}
\details{
This function fits a \code{ClassTopics} model using k-fold cross-validation, so
that we get an unbiased accuracy estimate with each observation appearing in
test sets only once.

Folds (and the final full-data fit) are run concurrently via
\code{future::multisession}; the previous \code{future::plan()} is restored on exit.

Returns:
\itemize{
\item cv_accuracy: Unbiased estimate of model performance
\item final_model: Model trained on ALL data (for interpretation)
}
}
